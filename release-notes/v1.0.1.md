# Outlook Part Sender v1.0.1 — Compatibility Hotfix

**Status:** Superseded

Removed WinForms `PlaceholderText` usage from recipient fields so the prototype could run on Windows PowerShell 5.1/.NET Framework environments where that property is unavailable.

## Asset

`Outlook_Part_Sender_v1.0.1.zip`
